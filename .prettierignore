# generated bundles — never reformat build output
**/dist/
**/*.min.js
**/*.min.css
node_modules/

# prose keeps its hand-written formatting
*.md
**/*.md

# demo pages are hand-authored, and their `<code>` samples are styled
# `white-space: pre` — Prettier's reflow turns them into one wrapped line
**/demo/
